const LogMessage = () =>{
     return(
        <div className = "log-wrapper">
        <p className = "log-message">Please Login to use the system.</p>
        </div>
     )
}
export default LogMessage;